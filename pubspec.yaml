name: macros_practice
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '^3.6.0-318.0.dev'

dependencies:
  flutter:
    sdk: flutter
  json: ^0.20.1
  macros: ^0.1.3-main.0 
  meta: ^1.15.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
